<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PgnTools.Views.WelcomePage"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="ToolCardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{ThemeResource SystemControlBackgroundBaseLowBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CardBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="CardBorder.BorderBrush" Value="{StaticResource AccentBrush}"/>
                                            <Setter Target="CardBorder.Background" Value="{StaticResource AccentLightBrushLow}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="CardBorder.BorderBrush" Value="{StaticResource AccentDarkBrush}"/>
                                            <Setter Target="CardBorder.Background" Value="{StaticResource AccentLightBrushLow}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="CardBorder.Opacity" Value="0.6"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentPresenter Padding="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Tools"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="0,0,0,16"/>

        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <controls:ItemsRepeater ItemsSource="{Binding Tools}">
                <controls:ItemsRepeater.Layout>
                    <controls:UniformGridLayout MinItemWidth="260"
                                                MinItemHeight="160"
                                                MinRowSpacing="16"
                                                MinColumnSpacing="16"
                                                ItemsJustification="SpaceEvenly"/>
                </controls:ItemsRepeater.Layout>
                <controls:ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource ToolCardButtonStyle}"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                Command="{Binding DataContext.OpenToolCommand, ElementName=Root}"
                                CommandParameter="{Binding}">
                            <StackPanel Spacing="10">
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <Grid Width="28" Height="28">
                                        <Image Source="{Binding IconAssetUri}"
                                               Width="28"
                                               Height="28"
                                               Stretch="Uniform"
                                               Visibility="{Binding HasIconAsset, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        <FontIcon Glyph="{Binding IconGlyph}"
                                                  Foreground="{StaticResource PrimaryBrush}"
                                                  FontSize="28"
                                                  Visibility="{Binding HasIconAsset, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                    </Grid>
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="18"
                                               Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Description}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           TextWrapping="Wrap"
                                           FontSize="15"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </controls:ItemsRepeater.ItemTemplate>
            </controls:ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>

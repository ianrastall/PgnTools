<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PgnTools.Views.Tools.Lc0DownloaderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:PgnTools.Views.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" NavigationCacheMode="Disabled">

    <Grid Padding="0,0,0,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,24">
            <TextBlock Text="Lc0" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="Download match PGNs from storage.lczero.org, tag them, and collate into monthly files."
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Inputs -->
        <StackPanel Grid.Row="1" Spacing="12" Margin="0,0,0,24">
            <TextBlock Text="Output Folder"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         PlaceholderText="Select output folder..."
                         Text="{x:Bind ViewModel.OutputFolderPath, Mode=OneWay}"
                         IsReadOnly="True"
                         Margin="0,0,12,0"/>

                <Button Grid.Column="1"
                        Content="Browse..."
                        Command="{x:Bind ViewModel.SelectOutputFolderCommand}"
                        Style="{StaticResource Action}"/>
            </Grid>

            <TextBlock Text="Month Range (optional)"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <StackPanel Orientation="Horizontal" Spacing="12">
                <CalendarDatePicker Header="Start Month"
                                    Date="{x:Bind ViewModel.StartDate, Mode=TwoWay}"/>
                <CalendarDatePicker Header="End Month"
                                    Date="{x:Bind ViewModel.EndDate, Mode=TwoWay}"/>
            </StackPanel>

            <CheckBox Content="Snap to full months (1st to last day)"
                      IsChecked="{x:Bind ViewModel.SnapToFullMonths, Mode=TwoWay}"/>

            <CheckBox Content="Show advanced options"
                      IsChecked="{x:Bind ViewModel.ShowAdvancedOptions, Mode=TwoWay}"/>

            <StackPanel Spacing="12"
                        Visibility="{x:Bind ViewModel.ShowAdvancedOptions, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="Max Pages (optional)"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBox PlaceholderText="Leave blank to scan until empty"
                         Text="{x:Bind ViewModel.MaxPages, Mode=TwoWay}"/>

                <TextBlock Text="Max Matches (optional)"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBox PlaceholderText="Leave blank for all matches"
                         Text="{x:Bind ViewModel.MaxMatches, Mode=TwoWay}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="Run"
                        Command="{x:Bind ViewModel.RunCommand}"
                        Style="{StaticResource PrimaryAction}"/>

                <Button Content="Cancel"
                        Command="{x:Bind ViewModel.CancelCommand}"
                        Style="{StaticResource Action}"
                        IsEnabled="{x:Bind ViewModel.IsRunning, Mode=OneWay}"/>
            </StackPanel>
        </StackPanel>

        <!-- Status -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <controls:ToolStatusBar StatusMessage="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                                    StatusSeverity="{x:Bind ViewModel.StatusSeverity, Mode=OneWay}"
                                    StatusDetail="{x:Bind ViewModel.StatusDetail, Mode=OneWay}"
                                    IsRunning="{x:Bind ViewModel.IsRunning, Mode=OneWay}"
                                    IsIndeterminate="{x:Bind ViewModel.IsIndeterminate, Mode=OneWay}"
                                    ProgressValue="{x:Bind ViewModel.ProgressValue, Mode=OneWay}"/>
        </ScrollViewer>
    </Grid>
</Page>

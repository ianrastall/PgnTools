<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PgnTools.Views.Tools.FilterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:PgnTools.Views.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" NavigationCacheMode="Disabled">

    <Grid Padding="0,0,0,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,24">
            <TextBlock Text="Filter" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="Apply general filters and cleanup rules: Elo, checkmates, annotations, variants, and ply count."
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Inputs -->
        <StackPanel Grid.Row="1" Spacing="12" Margin="0,0,0,24">
            <TextBlock Text="Input File"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         PlaceholderText="Select input PGN..."
                         Text="{x:Bind ViewModel.InputFilePath, Mode=OneWay}"
                         IsReadOnly="True"
                         Margin="0,0,12,0"/>

                <Button Grid.Column="1"
                        Content="Browse..."
                        Command="{x:Bind ViewModel.SelectInputFileCommand}"
                        Style="{StaticResource Action}"/>
            </Grid>

            <TextBlock Text="Output File"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         PlaceholderText="Select output PGN..."
                         Text="{x:Bind ViewModel.OutputFilePath, Mode=OneWay}"
                         IsReadOnly="True"
                         Margin="0,0,12,0"/>

                <Button Grid.Column="1"
                        Content="Browse..."
                        Command="{x:Bind ViewModel.SelectOutputFileCommand}"
                        Style="{StaticResource Action}"/>
            </Grid>

            <TextBlock Text="Elo Filter"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <StackPanel Orientation="Horizontal" Spacing="24">
                <StackPanel Spacing="6">
                    <TextBlock Text="Minimum Elo"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <NumberBox Width="180"
                               Minimum="0"
                               SmallChange="10"
                               SpinButtonPlacementMode="Inline"
                               Value="{x:Bind ViewModel.MinElo, Mode=TwoWay}"/>
                </StackPanel>

                <StackPanel Spacing="6">
                    <TextBlock Text="Maximum Elo"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <NumberBox Width="180"
                               Minimum="0"
                               SmallChange="10"
                               SpinButtonPlacementMode="Inline"
                               Value="{x:Bind ViewModel.MaxElo, Mode=TwoWay}"/>
                </StackPanel>
            </StackPanel>

            <CheckBox Content="Require both players to have Elo"
                      IsChecked="{x:Bind ViewModel.RequireBothElos, Mode=TwoWay}"/>

            <TextBlock Text="Ply Count Filter (half-moves)"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <StackPanel Orientation="Horizontal" Spacing="24">
                <StackPanel Spacing="6">
                    <TextBlock Text="Minimum Ply"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <NumberBox Width="180"
                               Minimum="0"
                               SmallChange="2"
                               SpinButtonPlacementMode="Inline"
                               Value="{x:Bind ViewModel.MinPlyCount, Mode=TwoWay}"/>
                </StackPanel>

                <StackPanel Spacing="6">
                    <TextBlock Text="Maximum Ply"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <NumberBox Width="180"
                               Minimum="0"
                               SmallChange="2"
                               SpinButtonPlacementMode="Inline"
                               Value="{x:Bind ViewModel.MaxPlyCount, Mode=TwoWay}"/>
                </StackPanel>
            </StackPanel>

            <CheckBox Content="Keep only checkmates"
                      IsChecked="{x:Bind ViewModel.OnlyCheckmates, Mode=TwoWay}"/>

            <CheckBox Content="Remove non-standard games (Variant/FEN)"
                      IsChecked="{x:Bind ViewModel.RemoveNonStandard, Mode=TwoWay}"/>

            <TextBlock Text="Strip Annotations"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <StackPanel Orientation="Horizontal" Spacing="24">
                <CheckBox Content="Comments"
                          IsChecked="{x:Bind ViewModel.RemoveComments, Mode=TwoWay}"/>
                <CheckBox Content="NAGs"
                          IsChecked="{x:Bind ViewModel.RemoveNags, Mode=TwoWay}"/>
                <CheckBox Content="Variations"
                          IsChecked="{x:Bind ViewModel.RemoveVariations, Mode=TwoWay}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="Run"
                        Command="{x:Bind ViewModel.RunCommand}"
                        Style="{StaticResource PrimaryAction}"/>

                <Button Content="Cancel"
                        Command="{x:Bind ViewModel.CancelCommand}"
                        Style="{StaticResource Action}"
                        IsEnabled="{x:Bind ViewModel.IsRunning, Mode=OneWay}"/>
            </StackPanel>
        </StackPanel>

        <!-- Status -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <controls:ToolStatusBar StatusMessage="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                                    StatusSeverity="{x:Bind ViewModel.StatusSeverity, Mode=OneWay}"
                                    StatusDetail="{x:Bind ViewModel.StatusDetail, Mode=OneWay}"
                                    IsRunning="{x:Bind ViewModel.IsRunning, Mode=OneWay}"
                                    IsIndeterminate="False"
                                    ProgressValue="{x:Bind ViewModel.ProgressValue, Mode=OneWay}"/>
        </ScrollViewer>
    </Grid>
</Page>







